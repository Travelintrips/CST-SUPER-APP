# Release Evidence Matrix

**Version:** RC-2.2  
**Last Updated:** 2026-07-24  
**Status:** ⛔ NO-GO — Evidence incomplete (see BLOCKED rows below)

> This matrix defines the evidence required for each release gate.
> A gate is only PASS when its evidence exists, is verifiable, and has been confirmed by the designated owner.
> No credential values may appear in this file or any evidence artifact.

---

## Evidence Matrix

| # | Requirement | Evidence Type | Generated By | PASS Criteria | Current Status |
|---|---|---|---|---|---|
| 1 | **917 Tests PASS** | JSON report / log | `pnpm run audit:customer-static` | Exit 0; "917 passed, 0 failed" in output | ✅ PASS |
| 2 | **Build PASS** | Build log | `pnpm run audit:customer-static` | Exit 0; all 4 packages built without error | ✅ PASS |
| 3 | **Typecheck PASS** | tsc log | `pnpm run audit:customer-static` | Exit 0; 0 TypeScript errors across all packages | ✅ PASS |
| 4 | **Runtime PASS** | JSON / log | `pnpm run audit:customer-runtime` | Exit 0; DB connected; workers running; health 200 | ✅ PASS |
| 5 | **Secret Availability PASS** | JSON report | `pnpm run audit:secrets` | Exit 0; PRESENT: N; MISSING: 0; INVALID: 0 | ✅ PASS |
| 6 | **Secret Rotation PASS** | `secret-rotation-status.json` | Account owner (manual) | Exit 0; all 19 credentials: `rotated=true`, `oldCredentialRevoked=true`, `verified=true`; `verifiedByOwner=true` | ⛔ INCOMPLETE |
| 7 | **Dedicated Staging Target** | Connectivity log | DevOps (manual) | `psql $TEST_DATABASE_URL -c "SELECT 1"` succeeds; staging project is NOT prod or shared dev | ⛔ NOT CONFIGURED |
| 8 | **HTTP E2E PASS** | JSON E2E report | `pnpm run audit:customer-http-e2e` | Exit 0; all 16 business scenarios PASS | ⛔ BLOCKED |
| 9 | **Accounting PASS** | E2E sub-report | `pnpm run audit:customer-http-e2e` | Journal immutability PASS; period lock PASS; debit = credit | ⛔ BLOCKED |
| 10 | **Tenant Isolation PASS** | E2E sub-report | `pnpm run audit:customer-http-e2e` | Company A cannot access Company B data — verified in E2E | ⛔ BLOCKED |
| 11 | **Security PASS** | E2E sub-report | `pnpm run audit:customer-http-e2e` | Auth 401, RBAC, token expiry, rate-limit 429 — all verified | ⛔ BLOCKED |
| 12 | **SSE PASS** | E2E sub-report | `pnpm run audit:customer-http-e2e` | SSE event received within timeout on staging | ⛔ BLOCKED |
| 13 | **Cleanup PASS** | E2E cleanup log | `pnpm run audit:customer-http-e2e` | All synthetic records tagged with `RUNTIME_TEST_RUN_ID` deleted | ⛔ BLOCKED |
| 14 | **Backup Verified** | Backup timestamp + restore log | DevOps (manual) | Supabase backup timestamp < 24h; restore tested on staging; owner sign-off | ⛔ NOT DONE |
| 15 | **Rollback Tested on Staging** | Manual screenshot / log | Release Lead (manual) | App rolled back to previous version on staging; health checks passed | ⛔ NOT DONE |
| 16 | **Monitoring Enabled** | Screenshot | DevOps (manual) | Uptime monitor active; alert channel confirmed | ⛔ NOT DONE |
| 17 | **Production Gate PASS** | `summary.json` + gate log | `pnpm run audit:customer-production` | Exit 0; `"production": "GO"` in `summary.json`; no blocker reasons | ⛔ NO-GO |
| 18 | **Owner Approval** | Sign-off (written) | Account Owner | Signed checklist in `docs/release/final-go-checklist.md` | ⛔ PENDING |
| 19 | **Technical Lead Approval** | Sign-off (written) | Engineering Lead | Signed checklist in `docs/release/final-go-checklist.md` | ⛔ PENDING |

---

## Evidence Format Requirements

| Evidence Type | Format | Storage Location | Retention |
|---|---|---|---|
| JSON report | `summary.json`, E2E JSON output | Workspace root / CI artifact | Per release |
| Log | stdout captured to file | CI log / Replit workflow log | Per release |
| Script | Executable script with exit code | `scripts/` directory | Permanent |
| Manual screenshot | PNG/JPG of browser or dashboard | `docs/release/evidence/` directory | Per release |
| Owner sign-off | Completed checklist with date + name | `docs/release/final-go-checklist.md` | Permanent |

---

## Evidence Rules

1. Evidence must be generated from a **dedicated staging environment** — not from shared dev or production.
2. Evidence artifacts must be **timestamped** (file modification time or embedded timestamp in JSON).
3. No credential values may appear in any evidence artifact.
4. Manual evidence (screenshot, sign-off) must be completed by the **designated owner** — not by an automated agent.
5. Evidence for a BLOCKED gate cannot be accepted from a dev-only run — only from the dedicated staging E2E harness.
6. `verifiedByOwner` in `secret-rotation-status.json` may only be set to `true` after `rotated`, `oldCredentialRevoked`, and `verified` are all `true` for every credential.

---

## How to Update This Matrix

When a gate transitions from BLOCKED/INCOMPLETE to PASS:

1. Update the **Current Status** column for that row.
2. Record the evidence artifact path/location in the **Evidence Type** column note.
3. Update `summary.json` (generated automatically by the gate script — do not edit manually).
4. Update the History section in `docs/release/release-readiness.md`.

**Do not mark any gate PASS without the corresponding verifiable evidence.**
